# 06 测试方案

## 1. 测试职责
- 组员 B 负责：接口测试用例编写、Postman 集合维护、接口回归
- 组员 A（后端）：JUnit 单元测试、Controller 冒烟测试
- 组员 B（前端）：关键页面操作核对清单（手动）

## 2. 后端单元测试

### 工具
- JUnit 5
- Mockito
- Spring Boot Test（@SpringBootTest）
- MockMvc（Controller 冒烟）

### 测试范围与覆盖率目标
| 层 | 范围 | 目标 |
|----|------|------|
| Service | 帖子发布、评论树构建、点赞去重、热门分计算、举报状态流转 | ≥60% |
| Controller | 5-10 个关键接口冒烟 | 路径覆盖 |
| Mapper | 复杂查询 | 不强制 |

### 测试源码位置
```
backend/src/test/java/com/school/forum/
├── service/
│   ├── PostServiceTest.java
│   ├── CommentServiceTest.java
│   ├── LikeServiceTest.java
│   ├── HotPostServiceTest.java
│   └── ReportServiceTest.java
└── controller/
    └── PostControllerTest.java
```

### 示例测试用例（PostService）
| 用例 | 输入 | 预期 |
|------|------|------|
| 正常发帖 | 有效 title/channel/tagIds | status=NORMAL，评论计数 0 |
| 频道被禁用 | 已禁用频道 | 抛 BusinessException |
| 标签超限 | tagIds = 6 个 | 抛参数异常 |

## 3. 接口测试（Postman）

### 集合结构
```
SchoolForum
├── Auth          (register/login/logout/me)
├── Channel       (tree/crud)
├── Tag           (list/hot/crud)
├── Post          (list/hot/detail/create/update/delete/top/essence)
├── Comment       (list/create/delete)
├── Like          (toggle/status)
├── Collect       (toggle/list)
├── Notification  (list/unread-count/read)
├── Report        (create/admin list/handle)
└── User/Admin    (profile/posts/collects/ban/unban)
```

### 环境变量
- `{{baseURL}}` = http://localhost:8080/api
- `{{token}}` = 登录响应中获取（脚本自动写入）

### 进度产出
- 导出文件：`docs/postman/SchoolForum.postman_collection.json`
- 环境文件：`docs/postman/Schoolforum.postman_environment.json`

### 用例表（Excel）
列：用例ID / 模块 / 接口 / 方法 / 前置条件 / 输入 / 预期响应 / 实际结果 / 通过

样例：

| ID | 模块 | 接口 | 方法 | 前置 | 输入 | 预期 | 结果 |
|----|------|------|------|------|------|------|------|
| AUTH-001 | Auth | /auth/login | POST | 已注册 | 正确账号密码 | code=0，data.token 非空 | 通过 |
| AUTH-002 | Auth | /auth/login | POST | — | 错误密码 | code=1003 | 通过 |
| POST-001 | Post | /posts | GET | — | page=1&size=20 | code=0，records 长度 ≤20 | 通过 |
| POST-002 | Post | /posts | POST | 已登录 | 有效数据 | code=0，postId>0 | 通过 |
| POST-003 | Post | /posts | POST | 未登录 | — | code=1001 | 通过 |

## 4. 手动测试核对清单（前端关键流程）

测试日期：2026-06-26 | 线上地址：http://182.92.245.148/

- [x] 游客可浏览首页 / 频道 / 详情（GET /api/posts, /api/channels, /api/posts/{id} 均返回 code=0）
- [x] 注册 → 登录 → 进入个人主页（POST /api/auth/register 线上返回 5001，本地测试通过，线上需排查 Auth 配置）
- [ ] 发帖 → 列表出现 → 详情可看 → 编辑 → 删除消失（需登录，线上 auth 异常，本地通过 CTRL_SMOKE_004/005 验证）
- [x] 评论 + 楼中楼回复可见（GET /api/comments/{postId} 返回树形结构）
- [ ] 点赞 / 收藏 可切换状态（需登录，本地 ServiceTest 已验证 toggle/status 逻辑）
- [ ] 通知：被评论后通知列表出现新消息（需登录，本地 ServiceTest 已验证）
- [ ] 举报：管理员后台可看到待处理记录（需登录 + ADMIN 角色）
- [ ] 管理员禁用频道 → 列表过滤（需 ADMIN 权限）
- [x] 热门榜：定时刷新后排序变化（GET /api/posts/hot 返回排序数据）

## 5. 测试环境
- 后端测试配置 `application-test.yml`
- 测试库 `ivy_forum_test`（独立，避免污染开发库）
- 数据库 MySQL 5.7.26，JDK 17，Spring Boot 3.2.5
- Postman 集合：`docs/postman/SchoolForum.postman_collection.json`
- Postman 环境：`docs/postman/SchoolForum.postman_environment.json`

## 6. 测试结果汇总

### 6.1 后端单元测试（JUnit 5 + Mockito + Spring Boot Test）

| 测试类 | 用例数 | 通过 | 失败 |
|--------|--------|------|------|
| AuthServiceTest | 4 | 4 | 0 |
| PostServiceTest | 6 | 6 | 0 |
| CommentServiceTest | 5 | 5 | 0 |
| LikeServiceTest | 4 | 4 | 0 |
| CollectServiceTest | 3 | 3 | 0 |
| NotificationServiceTest | 4 | 4 | 0 |
| ReportServiceTest | 4 | 4 | 0 |
| UserServiceTest | 4 | 4 | 0 |
| **Service 合计** | **34** | **34** | **0** |

### 6.2 Controller 冒烟测试（MockMvc）

| 测试用例 | 覆盖接口 | 结果 |
|----------|----------|------|
| CTRL_SMOKE_001 | GET /posts | ✅ |
| CTRL_SMOKE_002 | GET /posts/hot | ✅ |
| CTRL_SMOKE_003 | GET /posts/{id} | ✅ |
| CTRL_SMOKE_004 | POST /posts (create) | ✅ |
| CTRL_SMOKE_005 | DELETE /posts/{id} | ✅ |
| CTRL_SMOKE_006 | GET /posts/{id} (not found) | ✅ |
| CTRL_SMOKE_007 | GET /channels | ✅ |
| CTRL_SMOKE_008 | GET /channels/tree | ✅ |
| CTRL_SMOKE_009 | GET /tags | ✅ |
| CTRL_SMOKE_010 | GET /tags/hot | ✅ |
| CTRL_SMOKE_011 | GET /comments/{postId} | ✅ |
| CTRL_SMOKE_012 | POST /auth/register | ✅ |
| CTRL_SMOKE_013 | POST /auth/register + login | ✅ |
| CTRL_SMOKE_014 | POST /auth/login (wrong pwd) | ✅ |
| **Controller 合计** | **14** | **14 通过** |

### 6.3 线上接口回归（http://182.92.245.148/）

| 接口 | 结果 |
|------|------|
| GET /api/posts | ✅ code=0, 6 posts |
| GET /api/posts/hot | ✅ code=0, 排序正确 |
| GET /api/posts/{id} | ✅ code=0 |
| GET /api/posts/99999 | ✅ code=3001 (资源不存在) |
| GET /api/channels | ✅ code=0, 5 channels |
| GET /api/channels/tree | ✅ code=0 |
| GET /api/tags | ✅ code=0, 10 tags |
| GET /api/tags/hot | ✅ code=0 |
| GET /api/comments/1 | ✅ code=0 |
| POST /api/auth/register | ❌ code=5001 (服务器内部错误) |
| POST /api/auth/login | ❌ code=5001 (服务器内部错误) |

> ⚠️ **已知问题**：线上服务器 Auth 接口返回 5001，可能原因：数据库连接配置 / AuthenticationManager Bean 注入问题。建议检查线上 application.yml 配置。

### 6.4 测试覆盖率
- Service 层目标 ≥60%，实际覆盖帖子发布、评论树构建、点赞去重、举报状态流转等核心逻辑
- Controller 层 14 个关键接口冒烟覆盖

## 7. CI 集成（可选）
GitHub Actions / Gitee 流水线：
- 后端：mvn test（48 tests, 全部通过）
- 前端：npm run build
- 测试报告归档